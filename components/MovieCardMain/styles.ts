import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    mainContainer: {
        alignItems: 'center',
    },
    container: {
        overflow: 'hidden',
    },
    image: {
        width: '100%',
        height: '100%',
    },
});