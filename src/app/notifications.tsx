import { styles } from '@/src/screen/Explorer/styles';
import {
    SafeAreaView,
} from 'react-native';
import { NotificationsProps } from '../screen/Notifications/types';

export default function Notifications(props: NotificationsProps) {

    const {

    } = props;

    return (
        <SafeAreaView style={styles.container}>

        </SafeAreaView>
    );
}